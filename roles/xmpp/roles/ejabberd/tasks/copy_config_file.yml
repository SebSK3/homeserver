- name: "Create directory {{ docker_dir }}/{{ container_name }}/conf"
  become: true
  ansible.builtin.file:
    path: "{{ docker_dir }}/{{ container_name }}/conf"
    state: directory
    owner: "ejabberd"
    group: "ejabberd"
    mode: "0755"
- name: "Copy over templates"
  become: true
  ansible.builtin.template:
    src: 'templates/ejabberd.yml.j2'
    dest: "{{ docker_dir }}/{{ container_name }}/conf/ejabberd.yml"
    owner: "ejabberd"
    group: "ejabberd"
    mode: "0644"
  notify: Restart ejabberd
